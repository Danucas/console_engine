#include <stdio.h>
#include <stdlib.h>
#include "englibs.h"

extern int *dimensions;
void print_time()
{
	printf("%s CLOCK ", mov_coord(_tostring(dimensions[1] - 3), _tostring(dimensions[0] - 8)));

}
